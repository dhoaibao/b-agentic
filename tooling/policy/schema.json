{
  "version": 1,
  "top_level_required": [
    "policy_version",
    "status_block",
    "handoff_block",
    "cause_classes",
    "skill_verdicts",
    "readiness"
  ],
  "status_block_required": [
    "required_fields",
    "optional_fields",
    "state_values",
    "confidence_values",
    "run_id_format",
    "run_id_conditions"
  ],
  "handoff_block_required": [
    "required_fields",
    "optional_fields"
  ],
  "readiness_required": [
    "review_ready_for_pr_requires",
    "browser_evidence_prerequisites"
  ],
  "required_verdict_owners": [
    "b-review"
  ],
  "allowed_status_states": [
    "complete",
    "blocked",
    "needs-input",
    "handed-off"
  ],
  "allowed_confidence_values": [
    "high",
    "medium",
    "low"
  ],
  "cause_class_pattern": "^[a-z_]+$"
}
