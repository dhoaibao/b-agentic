{
  "permissions": {
    "allow": [
      "mcp(serena/*)",
      "mcp(codegraph/*)",
      "mcp(context7/*)",
      "mcp(brave-search/*)",
      "mcp(firecrawl/firecrawl_agent_status)",
      "mcp(firecrawl/firecrawl_check_crawl_status)",
      "mcp(firecrawl/firecrawl_extract)",
      "mcp(firecrawl/firecrawl_interact)",
      "mcp(firecrawl/firecrawl_interact_stop)",
      "mcp(firecrawl/firecrawl_map)",
      "mcp(firecrawl/firecrawl_parse)",
      "mcp(firecrawl/firecrawl_research_inspect_paper)",
      "mcp(firecrawl/firecrawl_research_read_paper)",
      "mcp(firecrawl/firecrawl_research_related_papers)",
      "mcp(firecrawl/firecrawl_research_search_github)",
      "mcp(firecrawl/firecrawl_research_search_papers)",
      "mcp(firecrawl/firecrawl_scrape)",
      "mcp(firecrawl/firecrawl_search)",
      "mcp(firecrawl/firecrawl_search_feedback)",
      "mcp(playwright/*)"
    ],
    "ask": [
      "mcp(firecrawl/firecrawl_agent)",
      "mcp(firecrawl/firecrawl_crawl)",
      "command(git commit.*)",
      "command(rtk git commit.*)",
      "command(git push.*)",
      "command(rtk git push.*)",
      "command(git pull.*)",
      "command(rtk git pull.*)",
      "command(git revert.*)",
      "command(rtk git revert.*)",
      "command(npm install.*)",
      "command(rtk npm install.*)",
      "command(pnpm install.*)",
      "command(rtk pnpm install.*)",
      "command(yarn install.*)",
      "command(rtk yarn install.*)",
      "command(bun install.*)",
      "command(rtk bun install.*)",
      "command(cargo install.*)",
      "command(rtk cargo install.*)",
      "command(go install.*)",
      "command(rtk go install.*)",
      "command(pip3? install.*)",
      "command(rtk pip3? install.*)",
      "command(poetry add.*)",
      "command(rtk poetry add.*)",
      "command(cargo add.*)",
      "command(rtk cargo add.*)",
      "command(go get.*)",
      "command(rtk go get.*)",
      "command(rm -rf.*)",
      "command(rtk rm -rf.*)"
    ],
    "deny": [
      "read_file(.env)",
      "read_file(.env.local)",
      "read_file(.env.production)",
      "read_file(.env.development)",
      "read_file(secrets/)",
      "read_file(credentials/)",
      "command(git reset --hard.*)",
      "command(rtk git reset --hard.*)",
      "command(git clean -f.*)",
      "command(rtk git clean -f.*)",
      "command(git push --force.*)",
      "command(rtk git push --force.*)",
      "command(git push --force-with-lease.*)",
      "command(rtk git push --force-with-lease.*)",
      "command(git branch -D.*)",
      "command(rtk git branch -D.*)",
      "command(docker system prune.*)",
      "command(rtk docker system prune.*)",
      "command(docker volume rm.*)",
      "command(rtk docker volume rm.*)"
    ]
  }
}
